In the following example, the _show_ action requires the user to be logged in.

**BookController.cfc**

	/**
 	 * @accessors true
	 */
	component {

		property _Book;

		/**
		 * @loggedIn
		 */
		function show() {

			params.book = _Book.get(params.id);

		}

	}