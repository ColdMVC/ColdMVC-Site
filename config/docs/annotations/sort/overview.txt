You can specify the default sort value for a model by adding the _@sort_ annotation to the model.

The _@sort_ annotation value is used as a default value when using  _list()_, _findAllBy()_, and _findAllWhere()_.